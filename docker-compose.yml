version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "80:80"
    volumes:
      - .:/var/www/html

#  mysql:
#    image: mysql:5.7
#    ports:
#      - "3306:3306"
#    env_file:
#      - .env